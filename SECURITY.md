# 安全策略

## 支持范围

安全修复以主分支当前发布版本为准。已归档或长期未维护的版本不提供安全更新。

## 漏洞报告

安全漏洞不应通过公开 Issue、讨论区或 Pull Request 披露。报告应发送至仓库维护者在 GitHub Profile 公开的安全联系方式，并包含受影响版本、影响描述、最小复现步骤及建议修复方案（如有）。

报告中不得包含生产环境地址、账号、密码、访问令牌、私钥、客户数据或其他敏感信息。

## 部署安全基线

- 使用 HTTPS，并设置 `PLATFORM_SESSION_COOKIE_SECURE=true`。
- 为 `PLATFORM_ADMIN_PASSWORD` 配置随机强密码，不使用示例值。
- 限制 `deploy.env`、密钥文件和产物目录的文件系统访问权限。
- 为 SSH、数据库和运行脚本配置最小权限账户。
- 将服务部署在受控网络，避免直接暴露管理接口至公共互联网。
- 定期更新 Python 依赖、容器基础镜像与前端依赖。
- 审核上传的本地脚本与远程执行目标，避免执行非受信任代码。

## 凭据处理

平台保存 SSH 连接信息以执行远程任务。生产环境宜使用密钥管理服务；在尚未接入密钥管理服务的部署中，应采用数据库加密、访问审计和严格的管理员权限控制。私钥、密码和令牌不得写入源码、镜像、日志、Issue 或 Git 历史。
