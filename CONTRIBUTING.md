# 贡献指南

## 开发流程

1. 基于默认分支创建功能分支。
2. 保持改动范围聚焦，并同步更新相关测试和文档。
3. 在提交前执行 `pytest`。
4. 通过 Pull Request 提交改动，并说明问题背景、实现方式和验证结果。

## 代码要求

- Python 代码保持现有的类型标注、命名和分层风格。
- 路由层负责 HTTP 输入输出，业务逻辑放在 `services/`。
- 不提交 `deploy.env`、数据库文件、运行产物、账号、密码、令牌、私钥或业务脚本。
- 新增依赖时，同步更新 `requirements.txt` 和相关许可证声明。

## Pull Request 要求

- 描述行为变化与兼容性影响。
- 关联已知 Issue（如适用）。
- 包含测试结果或无法测试的原因。
- 不混入格式化、重命名或无关重构。
